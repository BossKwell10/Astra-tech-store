{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            {{ form_start(form) }}
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title-custom">
                        <i data-feather="list"></i>
                        Modifier une catégorie
                    </h5>
                </div>
                <div class="card-body">
                    {{ include('category/_form.html.twig') }}
                </div>
                <div class="card-footer">
                    <button type="submit"
                            class="btn btn-secondary float-end">{{ button_label|default('Enregistrer') }}</button>
                    <a href="{{ path('app_category_index') }}" class="btn btn-info float-end me-2" style="color: white">Retour</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
